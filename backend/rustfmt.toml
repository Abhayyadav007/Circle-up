# Stable rustfmt options only (so `cargo fmt --check` passes in CI without nightly).
edition = "2021"
max_width = 100
use_small_heuristics = "Default"
newline_style = "Unix"
reorder_imports = true
reorder_modules = true
